---
import Common from "../components/Common.astro";

export const prerender = true

export function getStaticPaths() {
	return [
		{ params: { slug: 'a' } },
		{ params: { slug: 'b' } },
		{ params: { slug: 'c' } },
	];
}

---

<Common />
